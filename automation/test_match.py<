#!/usr/bin/env python3
import sys
sys.path.insert(0, "/run/media/pigeon/890dd7cb-9753-4f6b-b8e6-58c98e531754/Downloads/Schedule-Viewer-App-v2")
from automation.cleanup_detailers import match_name_to_detailer, load_canonical_detailers
import sqlite3
conn = sqlite3.connect("schedule.db")
c = load_canonical_detailers(conn)
for name in ["Matthew", "Tim", "Ryan", "Carl", "Brandon"]:
    r = match_name_to_detailer(name, c)
    print(f"  match_name_to_detailer({name!r:20s}) = {r!r}")